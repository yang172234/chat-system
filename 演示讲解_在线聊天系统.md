# 在线聊天系统 — 课程演示讲解

> **项目名称**：Chat System — 基于 Web 的实时在线聊天系统  
> **技术栈**：Node.js + Express + Socket.IO + SQLite + 原生 JavaScript  
> **适用场景**：Web 程序设计课程期末项目演示  

---

## 一、开场介绍（2分钟）

### 1.1 项目定位

大家好，我的课程项目是**在线聊天系统**——一个支持文字/语音消息、好友管理和群聊功能的实时 Web 聊天应用。

**核心定位**：麻雀虽小，五脏俱全。在约 4000 行代码内，完整实现了从用户认证、好友体系、实时消息到语音通信的全链路功能，没有使用任何前端框架，所有功能基于原生 Web API 和 JavaScript 实现。

### 1.2 为什么选这个项目？

> 建议口述时强调以下 3 点：

1. **技术覆盖面广**：涵盖 HTTP REST API、WebSocket 实时通信、数据库设计、前端状态管理、浏览器多媒体 API 等 Web 程序设计的核心知识点。
2. **实操性强**：B/S 架构 + 实时双向通信是当今互联网应用的主流模式（微信网页版、Slack、Discord 都是同类架构）。
3. **无框架依赖**：前端使用原生 JavaScript 实现 SPA，能清晰展示 DOM 操作、事件处理和状态管理的底层原理，不会被框架"黑盒"遮蔽。

---

## 二、系统架构概览（3分钟）

### 2.1 架构图

```
┌─────────────────────────────────────────────────────────┐
│                      浏览器 (SPA)                        │
│  ┌─────────┐ ┌─────────┐ ┌──────────┐ ┌───────────┐   │
│  │ auth.js │ │ chat.js │ │friends.js│ │ voice.js  │   │
│  └────┬────┘ └────┬────┘ └────┬─────┘ └─────┬─────┘   │
│       └───────────┴───────────┴─────────────┘           │
│              │  App 全局状态对象  │                        │
│              └──────────────────┘                        │
│              │ fetch() API  │  Socket.IO Client         │
└──────────────┼──────────────┼───────────────────────────┘
               │              │
           HTTP/REST      WebSocket
               │              │
┌──────────────┼──────────────┼───────────────────────────┐
│              ▼              ▼                            │
│         Express.js     Socket.IO Server                   │
│         (路由/中间件)    (事件/房间)           Node.js 服务端 │
│              │              │                            │
│              └──────┬───────┘                            │
│                     ▼                                    │
│              database.js                                 │
│              (sql.js WASM)                               │
│                     │                                    │
│                     ▼                                    │
│               chat.db (SQLite)                           │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术选型说明

| 技术 | 作用 | 选择理由 |
|------|------|----------|
| **Express.js** | HTTP 服务器 + REST API 路由 | Node.js 最广泛使用的 Web 框架，中间件生态成熟 |
| **Socket.IO** | WebSocket 实时通信 | 自动降级兼容（WS → 长轮询），内置房间/广播机制 |
| **SQLite (sql.js)** | 数据持久化 | 零配置、单文件存储、WASM 编译直接在 Node 进程中运行，无需安装数据库服务 |
| **express-session** | 用户会话管理 | 基于 Cookie 的服务端 Session，与 Socket.IO 共享会话状态 |
| **bcryptjs** | 密码哈希 | 防止明文存储，盐值轮次=10 |
| **原生 JavaScript** | 前端 SPA | 不依赖 Vue/React，便于展示底层 DOM 操作和状态管理原理 |

### 2.3 数据库 ER 图

```
┌──────────┐       ┌──────────────┐       ┌──────────┐
│  users   │       │  friendships │       │ friends  │
│──────────│       │──────────────│       │──────────│
│ id (PK)  │──┐    │ id (PK)      │    ┌──│ id (PK)  │
│ username │  │    │ user_id (FK) │────┘  │ name     │
│ password │  │    │ friend_id(FK)│       │ user_id  │
│ avatar   │  │    │ status       │       └──────────┘
│ online   │  │    │ group_id (FK)│
└──────────┘  │    │ message      │
              │    └──────────────┘
              │    ┌──────────────┐       ┌──────────────┐
              │    │group_members │       │   messages   │
              │    │──────────────│       │──────────────│
              │    │ group_id(FK) │────┐  │ id (PK)      │
              │    │ user_id (FK) │    │  │ from_id (FK) │──┐
              │    │ role         │    │  │ to_id (FK)   │──┤
              │    └──────────────┘    │  │ group_id(FK) │  │
              │    ┌──────────────┐    │  │ content      │  │
              │    │    groups    │    │  │ type         │  │
              │    │──────────────│    │  │ file_path    │  │
              └───►│ created_by  │◄───┘  │ created_at   │  │
                   │ name         │       └──────────────┘  │
                   └──────────────┘            ┌────────────┘
                                               └────── users
```

**6 张表的设计要点**：
- `users`：用户基础信息 + 在线状态
- `friendships`：好友关系（双向），包含状态机（pending/accepted/rejected）和分组关联
- `friends`：好友分组（如"我的好友"、"同事"、"家人"）
- `groups`：群组信息，记录创建者
- `group_members`：群组成员，区分 owner/member 角色
- `messages`：统一消息表，支持私聊（to_id）和群聊（group_id），支持 text 和 voice 两种类型

---

## 三、功能演示（8分钟）

### 3.1 用户认证模块

**演示要点**：
1. 注册页面：用户名 2-20 字符 + 密码 4 字符以上的前端验证 → 服务端二次验证 → bcrypt 哈希存储
2. 登录后 Session 持久化（24 小时有效），刷新页面自动恢复登录态（`POST /api/me`）
3. 登出后 Session 销毁

**关键代码片段**（`server.js` 注册接口）：

```javascript
app.post('/api/register', (req, res) => {
  const { username, password } = req.body;
  // 前端+后端双重验证
  if (!username || username.length < 2 || username.length > 20) {
    return res.status(400).json({ error: '用户名需 2-20 个字符' });
  }
  if (!password || password.length < 4) {
    return res.status(400).json({ error: '密码至少 4 个字符' });
  }
  // 哈希密码
  const hashedPassword = bcrypt.hashSync(password, 10);
  // 创建用户 + 默认好友分组
  const user = createUser(username, hashedPassword);
  res.json({ id: user.id, username: user.username });
});
```

### 3.2 好友系统

**演示要点**：
1. **搜索用户**：输入关键字 → 300ms 防抖 → 服务端 LIKE 查询 → 下拉结果列表
2. **发送好友申请**：可附带验证消息 → Socket.IO 实时通知对方
3. **处理申请**：接受/拒绝 → 双向好友关系建立 → 桌面通知（Notification API）
4. **好友分组**：右键好友 → 移动到不同分组（我的好友/同事/家人/同学）
5. **删除好友**：解除双向关系

**设计亮点**：
- 好友关系在 `friendships` 表中是**双向存储**的，确保查询效率
- 默认创建 4 个分组，新好友自动归入"我的好友"
- 申请状态机：`pending → accepted | rejected`

### 3.3 实时私聊

**演示要点**：
1. 点击好友 → 建立 Socket.IO 连接 → 进入 `user:{userId}` 房间
2. 发送消息 → Socket.IO `private-message` 事件 → 服务端存储 + 转发 → 对方实时收到
3. **打字指示器**：正在输入时发送 `typing-start` → 对方显示"xxx 正在输入..." → 停止输入 1.5 秒后发送 `typing-stop`
4. **消息历史**：光标分页加载（50 条/页），向上滚动自动加载更早消息
5. **导出聊天记录**：一键下载完整 JSON 文件

**关键代码片段**（Socket.IO 私聊消息处理）：

```javascript
socket.on('private-message', (data) => {
  const { toUserId, content, type } = data;
  const fromUserId = socket.request.session.userId;

  // 1. 存储到数据库
  const msg = savePrivateMessage(fromUserId, toUserId, content, type || 'text');

  // 2. 发送给接收方（对方在线则在 user:{toUserId} 房间内）
  const payload = { id: msg.id, fromUserId, toUserId, content, type, createdAt: msg.created_at };
  io.to(`user:${toUserId}`).emit('private-message', payload);

  // 3. 回显给发送方
  socket.emit('private-message', payload);
});
```

### 3.4 群聊功能

**演示要点**：
1. **创建群组**：输入群名 → 从好友列表勾选成员 → 创建成功
2. **群聊消息**：基于 Socket.IO 的 `group:{groupId}` 房间广播
3. **成员管理**：群主可邀请新成员 / 移除成员
4. **实时通知**：被加入群组时收到 `group-added` 事件通知
5. **右键查看成员**：右键群聊 → 弹窗显示成员列表及角色

### 3.5 语音消息

**技术链路演示**：

```
用户按住录音按钮
    │
    ▼
navigator.mediaDevices.getUserMedia({ audio: true })
    │
    ▼
MediaRecorder.start()  →  采集 WebM/Opus 音频数据块
    │
    ▼
用户松开按钮 → MediaRecorder.stop()
    │
    ▼
Blob 转 Base64 字符串
    │
    ▼
Socket.IO 发送 voice-message 事件
    │
    ▼
服务端：Base64 → Buffer → 写入 public/uploads/{uuid}.webm
    │
    ▼
数据库存储文件路径 → Socket.IO 转发给接收方
    │
    ▼
客户端渲染 <audio> 元素播放
```

**演示要点**：
1. 点击麦克风按钮开始录音 → 显示录音计时器
2. 松开按钮自动发送 → 对方收到可播放的音频消息
3. 支持私聊和群聊两种场景
4. 移动端触摸事件兼容

### 3.6 辅助功能

- **Emoji 选择器**：8 个分类约 200 个表情符号，点击插入光标位置
- **在线状态**：绿色圆点标识在线用户，实时更新
- **欢迎引导**：新用户首次登录显示 4 步操作指引

---

## 四、技术亮点深入讲解（4分钟）

### 4.1 HTTP + WebSocket 混合通信

**为什么需要两种协议？**

| 场景 | 协议 | 原因 |
|------|------|------|
| 注册、登录、搜索、好友操作、获取历史消息 | HTTP REST | 请求-响应模型，适合 CRUD 操作，语义清晰 |
| 发送消息、打字指示、好友通知、在线状态 | WebSocket (Socket.IO) | 需要服务端主动推送，低延迟，持久连接 |

**Session 共享的关键**：Socket.IO 通过中间件获取 Express Session，使 WebSocket 连接也能识别用户身份：

```javascript
io.use((socket, next) => {
  sessionMiddleware(socket.request, {}, next);
});
```

### 4.2 Room 模式实现消息路由

Socket.IO 的 Room 机制天然适合聊天场景：

- 每个用户上线时自动加入 `user:{userId}` 房间，服务端通过 `io.to('user:${id}')` 精确投递私聊消息
- 群聊使用 `group:{groupId}` 房间，成员打开群聊时加入，消息广播到房间内所有人
- 离开群聊界面时自动 `socket.leave()`，不会收到无关消息

### 4.3 光标分页替代偏移分页

传统 `LIMIT 50 OFFSET 100` 在实时聊天场景有问题：加载期间新消息插入会导致数据重复或遗漏。

本项目使用 **cursor-based pagination**（游标分页）：

```sql
SELECT * FROM messages
WHERE (to_id = ? OR from_id = ?)
  AND id < ?        -- 游标：上次加载的最早消息 ID
ORDER BY id DESC
LIMIT 50
```

无论期间插入多少新消息，分页结果都保持正确。

### 4.4 sql.js — 零依赖数据库方案

sql.js 是 SQLite 的 WebAssembly 编译版本，可以直接在 Node.js 进程中运行：
- **优点**：无需安装 MySQL/PostgreSQL 服务，`npm install` 即用，便于演示和部署
- **数据持久化**：每次写操作后调用 `db.export()` 导出二进制 → `fs.writeFileSync()` 写入 `chat.db`
- **缺点**：不适合高并发场景（本项目作为课程演示绰绰有余）

### 4.5 前端 SPA 架构

所有页面切换通过 DOM 操作实现，核心机制：

```javascript
// app.js — 全局状态管理
const App = {
  currentUser: null,
  socket: null,
  activeChat: { type: null, id: null },
  friends: [],
  groups: [],
  contacts: [],
  // ...
};

// 统一的 API 调用封装
async function api(path, options = {}) {
  const res = await fetch(path, {
    headers: { 'Content-Type': 'application/json' },
    ...options,
  });
  return res.json();
}

// 模态弹窗抽象
function showModal(title, bodyHtml, footerHtml) { /* ... */ }
```

---

## 五、项目总结（2分钟）

### 5.1 实现的功能清单

| 模块 | 功能 |
|------|------|
| 🔐 用户认证 | 注册、登录、登出、Session 持久化、密码哈希 |
| 👥 好友管理 | 搜索、申请、接受/拒绝、分组管理、删除 |
| 💬 私聊 | 实时文字消息、打字指示器、消息历史分页、导出聊天记录 |
| 👨‍👩‍👧 群聊 | 创建群组、邀请/移除成员、群消息、成员列表查看 |
| 🎤 语音消息 | 浏览器录音、WebM 编码、实时发送播放 |
| 😊 Emoji | 8 分类 ~200 表情选择器 |
| 🟢 在线状态 | 实时在线/离线广播 |
| 🔔 通知 | Socket.IO 实时推送 + 浏览器桌面通知 |

### 5.2 技术收获

1. 掌握了 HTTP REST API 的设计规范和最佳实践
2. 理解了 WebSocket 实时通信的原理和 Socket.IO 的 Room/Event 机制
3. 实践了 SQLite 数据库设计：表结构、外键、索引、游标分页
4. 学会了浏览器 Web API 的综合运用：MediaRecorder、Notification、Fetch、DOM 操作
5. 体会了前后端分离架构中 Session 共享、状态管理的工程挑战

### 5.3 改进方向

| 方向 | 计划 |
|------|------|
| 📁 文件/图片发送 | 扩展消息类型，支持图片、文件传输 |
| 🔒 端到端加密 | 使用 Web Crypto API 实现 E2E 加密 |
| 📱 PWA 支持 | Service Worker + Manifest 实现离线使用和桌面安装 |
| 🗄️ 数据库升级 | 替换为 PostgreSQL，支持更高并发 |
| 🧪 测试覆盖 | 添加单元测试和 E2E 测试 |

---

## 六、演示操作流程（给老师看）

### 推荐演示步骤

1. **启动项目**：`npm start`，浏览器打开 `http://localhost:3000`
2. **注册两个账号**：开两个浏览器窗口（或一个正常窗口 + 一个隐私窗口），分别注册用户 A 和用户 B
3. **添加好友**：A 搜索 B 并发送好友申请 → B 收到通知并接受
4. **私聊演示**：A 和 B 互相发消息 → 展示实时送达和打字指示器
5. **群聊演示**：A 创建一个群组，邀请 B → 群内发消息
6. **语音消息**：A 录制并发送一条语音 → B 点击播放
7. **导出聊天记录**：导出 JSON 文件并打开查看
8. **在线状态**：关闭 B 的窗口 → A 看到 B 变为离线

---

> 📌 **演示用时预估**：总计约 20 分钟（含操作演示），可根据老师要求灵活调整节奏。
